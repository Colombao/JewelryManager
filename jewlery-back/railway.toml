# Railway — backend (jewlery-back)
# Start: apenas node src/server.js (NÃO rode seed:auth no deploy)

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
