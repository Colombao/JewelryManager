# Railway — serviço do backend (jewlery-back)
# Monte um Volume no painel em /data e defina UPLOAD_ROOT=/data

[build]
builder = "NIXPACKS"

[deploy]
startCommand = "node src/server.js"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10
