# Railway — backend (jewlery-back)
# Node 20+ obrigatório (cheerio/puppeteer/undici usam global File)

[build]
builder = "NIXPACKS"

[build.env]
NIXPACKS_NODE_VERSION = "20"

[deploy]
startCommand = "npm run start"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10